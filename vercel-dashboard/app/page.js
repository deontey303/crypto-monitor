async function state(){try{const r=await fetch(process.env.AGENT_STATE_URL||"https://crypto-monitor-production-33be.up.railway.app/api/state",{cache:"no-store"});if(!r.ok)throw new Error("upstream "+r.status);return {ok:true,data:await r.json()}}catch(e){return {ok:false,error:String(e)}}} export default async function Page(){const s=await state(),d=s.data||{},sig=d.signal||d.latestSignal||d.latest||{},ev=sig.evidence||d.evidence||{};return <main><header><div><b>CRYPTO AGENT</b><span className="live">{s.ok?"● LIVE":"● DEGRADED"}</span></div><small>Production observability · read only</small></header><section className="hero"><div><label>ACTION</label><h1>{sig.action||d.action||"UNKNOWN"}</h1></div><div><label>BTC</label><h2>{sig.entry?("$"+Number(sig.entry).toLocaleString()):"—"}</h2></div><div><label>HORIZON</label><h2>{sig.horizon||"4h"}</h2></div><div><label>STRENGTH</label><h2>{sig.confidence??sig.decisionStrength??"—"}</h2><small>uncalibrated unless marked otherwise</small></div></section><h3>Evidence</h3><section className="grid">{["technical","derivatives","fundamental","news","macro","onchain"].map(k=><article key={k}><label>{k}</label><strong>{ev[k]??sig.scores?.[k]??"—"}</strong></article>)}</section><section className="details"><p><b>Signal ID</b> {sig.signal_id||sig.id||"—"}</p><p><b>Observed</b> {sig.observed_at||sig.created_at||d.timestamp||"—"}</p><p><b>Invalidation</b> {sig.invalidation||"not supplied"}</p><p><b>Targets</b> {Array.isArray(sig.targets)?sig.targets.join(" · "):(sig.targets||"not supplied")}</p></section>{!s.ok&&<section className="error"><b>UPSTREAM DEGRADED</b><p>{s.error}</p><p>No stale state is presented as live.</p></section>}<footer>Railway agent → read-only state → Vercel UI</footer></main>}